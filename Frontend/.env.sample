VITE_KEY = 
# Optional. If not set, /api is proxied to http://localhost:<PORT> (default 5000)
VITE_PROXY = 
# Optional. Vite dev server port. Defaults to 5173.
VITE_DEV_PORT = 

VITE_GOOGLE_CLIENT_ID =